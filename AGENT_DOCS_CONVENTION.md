# 소스 우선 에이전트 문서 규약 v2

Genre: contract
Canonical for: 에이전트 문서의 정보 소유권, 최소 읽기, 유지·검증·채택 원칙

문서는 코드에서 바로 얻을 수 없는 결정 정보를 제공한다. 목표는 파일 수나 문장 길이가 아니라 **작업에 필요한 읽기·탐색·유지 비용의 감소**다. 구현을 산문으로 다시 설명하고 독자가 코드로 재검증하게 만드는 층은 만들지 않는다.

## 1. 사실의 소유권

| 정보 | 정본 | 문서의 역할 |
|---|---|---|
| 구현 동작·상수·필드·분기·호출 관계 | 현재 소스·설정·스키마 | 첫 소유 경로/심볼만 안내 |
| 제품 요구·안전 경계·호환 의무·미구현 목표 | 해당 승인 계약 | 조건과 이유를 직접 보존 |
| 검사의 실행 방법·실패 조건 | 테스트·검사기·CI, 필요한 실행 매뉴얼 | 변경별 검증 진입점과 증명 한계 |
| 구조·책임·소유 경계 | 필요한 아키텍처 계약 | 여러 파일을 묶는 제약, 진입 앵커 |
| 진행 중인 작업·블로커 | 필요한 경우 단일 열린 상태 문서 | 아직 남은 것과 다음 진입 조건만 |
| branch·HEAD·dirty·실행 결과 | live Git·해당 실행 | 활성 문서에 현황 복제 금지 |
| 완료 이력·조사·시행착오 | Git 등 작업 기록 | 활성 진입에서 제외 |

**소스 우선은 구현 사실에 적용한다.** 코드에 없다는 이유로 승인 요구를 삭제하지 않는다. 계약과 구현의 불일치는 결함·의도 변경·낡은 문서 중 무엇인지 판단한다. 판단되지 않으면 충돌을 드러내고, 테스트 통과를 위해 계약을 완화하지 않는다.

## 2. 남길 정보와 버릴 정보

남기는 것은 제품 범위, 비자명한 불변식, 권한·안전·호환 경계, 선택의 이유, 반복하면 손해인 설계의 거절 조건, 실제 작업 진입점이다. 제약은 적용 범위·요구·이유를 포함하고 다시 검토할 조건이 있으면 함께 쓴다. 특정 세 줄 형식이나 제목은 의무가 아니다.

함수별 동작 해설, 코드 순회 보고, 상수·enum·테스트 목록/개수, 복사한 폴더 트리, 실행 성공 집계, 패치·세션 일지는 쓰지 않는다. 단순 검색으로 복원되는 값은 경로를 준다. 외부 wire/storage 규격처럼 문서가 요구 자체를 정의하는 경우는 보존하며, 스키마가 그 요구의 정본이면 스키마를 가리킨다.

여러 파일의 수명·책임·순서 제약은 계약에, 한 파일 안에서만 의미 있는 비자명한 이유는 인접 주석에 둔다. 컴파일러·테스트로 표현할 수 있는 불변식은 실행 가능하게 만들고 산술을 문서에 다시 쓰지 않는다. 주석에 날짜·패치 버전·작업 경위를 매번 추가하지 않는다.

## 3. 로컬 진입과 최소 읽기

로컬 `AGENTS.md`는 프로젝트 정체, 작업 권한, 필요한 안전 경계, 작업별 라우터, 검증/종료 지침을 제공한다. 일반 개발에 중앙 레포 접속이나 복제본을 요구하지 않는다. 중앙 규약은 문서체계 변경·신규 채택·버전 업그레이드 때만 조회한다.

기본 흐름은 **AGENTS → 해당 작업 행 → 필요한 계약 절 + 첫 소스 소유 지점 + 관련 검사**다. 모든 문서·overview·walkthrough의 선독을 첫 세션이라는 이유만으로 강제하지 않는다. 전체 아키텍처를 이해해야 하는 작업에서는 필요한 범위만 읽는다. 진행 중 구현의 순서·블로커가 실제 판단을 바꿀 때만 열린 상태를 먼저 읽는다.

시작 시 실제 root/branch/HEAD/status와 관련 변경을 확인하고 사용자 작업을 보존한다. 문서에는 특정 로컬 HEAD·프로세스·장치 번호를 고정하지 않는다. 규격 pin·upstream baseline·호환 기준·배포 식별처럼 안정적인 참조가 필요한 값은 예외이며 명확한 소유 위치에 둔다.

범용 모델에게 코드를 읽는 방법을 장문으로 가르치지 않는다. 짧게 쓰되 식별자를 과도하게 축약하거나 긴 단일 행에 압축하여 가독성을 해치지 않는다. 줄 수·토큰 예산은 검토 신호이지 강제 합격 수치가 아니다.

## 4. 파일·라우터·어댑터

작은 레포는 AGENTS 안의 라우터로 충분하다. 별도 MAP, STATUS, PLAN, ADR, 장르 디렉터리는 실제 소유 정보가 있을 때만 둔다. 개수 기준으로 파일을 나누거나 빈 표준 파일을 만들지 않는다. 서로 다른 언어·배치는 허용한다.

| 작업 | 필요한 계약 절 | 첫 소스/설정 앵커 | 관련 검증 |
|---|---|---|---|
| 로컬에서 정의 | 편집 전에 필요한 것만 | 구현을 찾는 경로/심볼 | 명령 소유 위치 또는 실행 명령 |

표의 모양보다 **왜 읽는지의 구별**이 중요하다. 계약·조회·검증을 한 필독 목록으로 합치지 않는다. 모든 활성 에이전트 문서는 진입에서 도달 가능해야 하지만 도달 가능하다는 이유로 모두 읽지는 않는다. 포인터는 경로+심볼/제목을 우선한다. 영구 문서의 줄 번호 의존과 파일 전체를 복제하는 지도는 피한다.

각 문서의 역할과 유일한 소유 범위를 드러낸다. 기존 `Genre:` / `Canonical for:`를 유지해도 되고 동등하게 분명한 제목·표를 사용해도 된다. 기본 역할은 contract / map / manual / state / entry다. 역할 혼합을 막기 위한 표식이지 문장 형태를 검사하는 문법이 아니다. 읽기 조건은 라우터 한 곳이 소유한다.

도구별 adapter는 로컬 AGENTS와 관련 경로로 연결한다. 공통 정책의 재복사, 의무 전체 문서 import, 세션마다 생성하는 컨텍스트, 읽기/검색 전 훅을 추가하지 않는다. 설치·빌드 환경은 실제 프로젝트 필요에 따라 명시하되, 발견되지 않은 머신 설정을 있다고 가정하거나 전역 지침을 임의 변경하지 않는다.

## 5. 현재 상태와 유지

상태 문서에는 미구현·블로커·미판정·다음 진입 조건만 둔다. 완료되면 지우거나 지속되는 요구만 원래 소유 위치에 반영한다. 완료 체크리스트, 닫힌 계획, 과거 ADR, 세션 인계 자료를 활성 archive로 누적하지 않는다. 현재도 결정을 제한하는 ADR의 이유·거절은 남기되 연대기를 쓰지 않는다.

고유 계약·이유는 삭제 전에 소유 위치를 보존한다. 지워진 식별자 자체는 보존 의무가 아니다. 소스가 이미 소유하는 토큰이나 끝난 작업 기록에 새 문서 집을 만들지 않는다. Git 삭제는 보통 복원 가능하지만 축약 이력에서도 필요한 현재 제약은 commit 메시지에만 맡기지 않는다.

문서·포인터·관련 검사는 같은 관심사 변경에서 정렬한다. 사용자 안내, upstream 문서, 라이선스/출처, 재현에 필요한 fixture, 배포·안전 판정에 실제 필요한 최소 증거는 별도 독자·용도다. 에이전트 컨텍스트 축소를 이유로 무조건 삭제하지 않고 기본 선독에서 분리한다.

## 6. 검증과 비용

로컬 문서 검증은 실제 사용한 링크/앵커, 활성 문서 도달성, 선언한 소유권 형식을 확인한다. 무효 경로·빈 입력·읽기 실패를 PASS로 취급하지 않는다. 검사기 변경에는 정상 입력과 의도적으로 깨진 fixture를 포함한다. 외부 경로를 건너뛰면 미검증으로 구별한다.

불필요한 파생 표를 없앨 때 그 표와 비교만 하던 assertion도 함께 제거한다. 실제 동작·메뉴 지원·wire·호환성·안전·입력 거절 검사는 유지한다. 숫자 일치와 링크 존재는 문장 의미나 제품 요구 준수를 증명하지 않는다. 생성 표를 유지할 구체적 독자가 있으면 생성 경로가 소유하며 손으로 복제하지 않는다.

문서만 변경하면 관련 문서 검사와 diff/공백 검토가 기본이다. 문서가 생성기 입력이거나 검사기·제품 계약을 바꾸면 실제 영향에 맞는 검사를 추가한다. 전체 빌드·HIL·다른 레포 전수 조사는 기본값이 아니다. 검증 범위의 source/config/dependency가 같고 결과가 유효하면 재사용한다. 충돌·불완전 결과·명시적 독립 검토가 있으면 필요한 부분을 재검증한다.

CI·버전관리된 선택적 commit 훅에서 실행하며 일반 읽기·검색 호출에 끼워 넣지 않는다. 검사 대상은 실제 commit 또는 명시한 작업 범위와 일치해야 한다. 무관한 사용자 변경까지 stage·삭제·stash해서 검사를 통과시키지 않는다. skip·unknown·NOT RUN·실기기 미측정을 PASS로 바꾸지 않는다.

## 7. 교차 레포 계약

소비 레포는 필요한 짝에 대해 **인터페이스/사실의 소유자 → 상대 소비 위치 → 호환성 확인 방법**만 연결한다. 중앙 레포에 프로젝트별 값·파일 목록·현재 채택 상태를 복제하지 않는다. 소유자가 다른 펌웨어를 같은 프로토콜로 억지 통합하지 않는다.

다른 레포는 그 AGENTS부터 읽고, 접근 권한과 현재 작업을 보존한다. 링크 표만으로 양쪽 호환을 검증했다고 쓰지 않는다. 실제 paired 검사에서는 각 입력 revision/작업 트리와 비교 범위를 실행 결과에 남긴다. 상대 부재나 권한 제한은 명시적 미검증이며, 로컬 검사 PASS와 구분한다. peer 변경·배포는 현재 사용자 요청의 범위를 넘지 않는다.

## 8. 버전과 채택

소비 레포의 로컬 진입 문서 한 곳에 규약 계열과 검토한 태그 또는 commit으로 고정한 파일 URL을 선언한다. `main` 추적·자동 fetch·전역 지침 강제 설치는 하지 않는다. 공개 태그는 이동하지 않고 수정판은 새 버전으로 낸다. 중앙을 수정해도 기존 소비 레포의 지침은 자동으로 바뀌지 않는다.

로컬 AGENTS는 해당 프로젝트에 적용할 구체적 작업 지침을 직접 제공한다. 공통 규약 전체를 복사하지 않는다. 문서 정비 때 선언한 고정본과 로컬 규칙·검사·어댑터를 비교하고 함께 정렬한 뒤 채택을 확정한다. 단순 링크 교체는 채택 완료가 아니다. 세부 절차는 [ADOPTION](ADOPTION.md)에 있다.

현재 사용자 지시와 유효한 제품·안전 경계를 우선한다. 공용 문서나 그 링크는 실행 권한을 부여하지 않으며 명시된 대상 밖의 push·배포·설치·삭제·실기기 조작을 승인하지 않는다. 의미가 충돌하는 로컬 예외만 범위·이유·재검토 조건을 남긴다.

## 9. 완료의 판정

작업에 필요한 제약·첫 소스 소유자·관련 검증이 라우터에서 결정되고, 구현 사실의 두 번째 사본이 없으며, 유효한 요구·이유·안전 경계가 보존되어야 한다. 로컬만으로 일반 개발이 가능하고 관련 문서 검사가 통과해야 한다.

압축률·문서 수·모델의 자기평가로 완료 처리하지 않는다. 실제 바꾼 범위·검증·미검증·남은 충돌을 작업 응답에 보고하며 또 다른 상시 보고서를 만들지 않는다.
